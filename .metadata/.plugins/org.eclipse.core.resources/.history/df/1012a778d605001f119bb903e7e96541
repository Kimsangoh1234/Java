package com.kh.AddressEX;
/*
Socket : 컴퓨터에서 통신을 하게 해주는 역할을 함

메서드 
 생성자 
 	socket():기본생성자 사용자가 소켓을 생성할 때 사용
이외 메서드
	connect():클라이언트가 서버에 연결하기위해 사용
			ex) 현관 앞에서 초인종을 누름
	accept() : 서버가 클라이언트의 연결 요청을 수락하고 클라이언트와 통신을 위한 소켓을 형성
			ex) 집 주인이 문을 열어줌
	send() 	 : 대화를 보냄
	receive() : 대화를 받음
	
	getInputStream() : 소켓에서 데이터(글자, 파일 등)을 읽어오기 위한 입력 스트리밍
	getoutputStream() : 소켓에서 가져온 데이터(글자,파일 등)을 쓰기 위하 출력 스트리밍
	
	close() : 소켓을 닫아 연결을 종료
BufferReader	
	readLine() : 텍스트를 한줄씩 읽어올 때 사용 대화나 파일 주고 받을 때 사용
	
serverSocket
	메서드 중에서ㅇ 우리집에 어떻게 들어올지 포트번호르 지정하는 필수 생성자로 많이 사용
printWriter 
	글로 작성한 내용 중
	
	
 */
public class SocketEx {

}
